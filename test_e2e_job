pipeline{
  agent any
  parameters {
    string(name: 'ENV_URL', defaultValue: '')
  }
  stages{
    stage('integration tests'){
      echo "npx cypress run --baseUrl ${params.ENV_URL}"
      sh 'npx cypress run --baseUrl ${params.ENV_URL}'
    }
  }
}
