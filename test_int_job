pipeline{
  agent any
  parameters {
    string(name: 'URL_ENV', defaultValue: '')
  }
  stages{
    stage('integration tests'){
      steps{
        sh 'npx cypress run --baseUrl ${params.URL_ENV}'
      }
    }
    //Solution Downstream
    stage('deploy to recette'){
      steps{
        script{
          build job: 'deploy_rec_job'
        }
      }
    }
  }
}
